# EXEMPLE UNIQUEMENT — ne pas commiter de vraies valeurs.
apiVersion: v1
kind: Secret
metadata:
  name: minio-creds
  namespace: velibdata
type: Opaque
stringData:
  MINIO_ROOT_USER: CHANGE_ME
  MINIO_ROOT_PASSWORD: CHANGE_ME_WITH_A_LONG_RANDOM_PASSWORD
